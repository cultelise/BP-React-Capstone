import React from 'react';
import SearchBar from './SearchBar';
import Display from './Display';

const Main = () => {
	return (
		<div>
			<SearchBar />
			<Display />
		</div>
	);
};

export default Main;
